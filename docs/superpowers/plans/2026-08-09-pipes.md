# Пайпы в AOS: pipefs, pipe() syscall, shell pipelines

> **Для агентных исполнителей:** ОБЯЗАТЕЛЬНЫЙ саб-скилл: используйте superpowers:subagent-driven-development (рекомендуется) или superpowers:executing-plans для пошаговой реализации этого плана. Шаги используют синтаксис чекбоксов (`- [ ]`) для отслеживания.

**Goal:** Реализовать настоящие пайпы: виртуальную ФС pipefs в ядре, syscall `pipe()` (Linux ABI 42) и произвольные цепочки `a | b | c` в kernel shell (каждая часть — отдельная задача, параллельное выполнение, блокирующие read/write в ядре).

**Architecture:** Дизайн из спеки `docs/superpowers/specs/2026-08-09-pipes-design.md`. `kernel/pipe.c` предоставляет pipefs — статический массив из 8 кольцевых буферов (PIPE_BUF_SIZE 4096) со счётчиками читателей/писателей. Концы пайпа — это слоты глобальной `ofiles[]` в VFS, указывающие на встроенный `struct vfs_inode` (`valid=0`, вне кеша). Освобождение слота пайпа — через новый хук `close` в `struct vfs_fs`. Read/write блокируются циклами `sti; hlt; cli` (задача остаётся `TASK_RUNNING`, планировщик round-robin её перезапускает); EOF = 0 при `nreaders==0`, EPIPE = -32 при `nreaders==0` на записи. Shell (`commands.c`) сплитит строку по `|`, спавнит каждую часть как задачу с подвязанными `stdin_fd`/`stdout_fd` и ждёт все части через `task_waitpid`.

**Tech Stack:** C11 kernel (i386, freestanding, `-Wall -Wextra`), static musl i386 toolchain (`tools/musl-i686/`), QEMU, Python3 E2E-скрипты.

## Global Constraints

- `PIPE_BUF_SIZE = 4096`, `PIPE_MAX = 8` — кольцевой буфер с `head`/`tail`/`count`, счётчики `nreaders`/`nwriters`.
- Pipe inode: `valid=0` (НЕ в кеше VFS), `ino = index+1`, `type=1`, `nlink=1`, `refcount=2`, `fs = &pipefs_fs`.
- Хук в `struct vfs_fs`: `void (*close)(struct vfs_fs *fs, unsigned int ino, int flags);` — NULL у sfs2/procfs (designated initializer), не трогаем их.
- `int vfs_pipe(int *rd, int *wr)` в `kernel/vfs.c`: ищет два свободных слота `ofiles[]` начиная с fd 3; нет слота → `VFS_EMFILE` (-24); при ошибке вернуть уже занятый слот.
- `pipe_read_at`: `while (count == 0 && nwriters > 0) { sti; hlt; cli; }`; EOF = 0 при `count == 0 && nwriters == 0`; `len == 0` → 0 без блокировки.
- `pipe_write_at`: пишет все `len` байт; `nreaders == 0` → `-32` (EPIPE); `len == 0` → 0.
- `pipe_stat`: `type=1`, `size=count`, `nlink=1`, `mtime=0`.
- Заглушки pipefs (защита от NULL-вызова при openat/fstatat64 с pipe-dirfd): `lookup`/`readdir`/`add_dirent`/`remove_dirent`/`mkdir`/`rmdir`/`unlink` → `VFS_ENOTDIR` (-20), `truncate` → `VFS_EINVAL` (-22), `alloc_inode` → 0.
- `route_text` меняет тип на `int`; возвращает `vfs_write_fd(...)` когда `stdout_fd >= 0`, иначе 0.
- SYS_WRITE (AOS) fd 1/2 и Linux write fd ≤ 2: `r->eax = (rc < 0) ? rc : count`. writev: EPIPE игнорируется (сумма длин).
- read(0): если `t->stdin_fd >= 0` → `vfs_read_fd(t->stdin_fd, buf, len)` (блокирующий), в ОБОИХ ABI-путях (AOS `syscall.c`, Linux `linux_syscall.c`). `terminal_read_key` НЕ менять.
- `pipe()` syscall = только Linux ABI `case 42` (musl делает pipe2→ENOSYS fallback на pipe). Реализовать 331 не нужно.
- `exec_pipe`: сплит по `|` (через `find_operator`); builtin в части или redirect (`>`/`<`) в части → ошибка без спавна; все команды резолвятся через `path_resolve` ДО создания пайпов; N-1 `vfs_pipe`; `task_spawn` всех частей; после spawn подвязка `c->fds[fd] = vfs_ofile_ptr(fd)` + `stdin_fd`/`stdout_fd`; провал spawn → `vfs_close_fd` всех концов; `task_waitpid` по порядку; `$?` = код последней части.
- Известные ограничения (вне скоупа, не реализуем): dup не отслеживается, SIGPIPE нет (EPIPE пробрасывается), busy-poll ≤1 мс, пайпы в фоне (`a | b &`), GUI-терминал, pipe2, AOS-ABI pipe.
- Документация (спека/план/AGENTS.md) — на русском; код, коммиты и комментарии в коде — на английском.
- Никаких новых предупреждений `-Wall -Wextra` при сборке.

---

### Task 1: Хук `close` в VFS

**Files:**
- Modify: `kernel/vfs.h` (struct vfs_fs)
- Modify: `kernel/vfs.c:465-472` (vfs_close_fd)

**Interfaces:**
- Consumes: существующая структура `struct vfs_fs`, `struct open_file` (`ofiles[]`), `VFS_EBADF`.
- Produces: новое поле `void (*close)(struct vfs_fs *fs, unsigned int ino, int flags);` в `struct vfs_fs`; `vfs_close_fd` вызывает его после `vfs_put`. Task 2 кладёт туда `pipe_close`.

- [ ] **Step 1: Добавить поле hook в `struct vfs_fs`**

В `kernel/vfs.h` добавить поле в конец структуры (после `alloc_inode`):

```c
    int (*alloc_inode)(struct vfs_fs *fs, unsigned int type);
    void (*close)(struct vfs_fs *fs, unsigned int ino, int flags);
```

- [ ] **Step 2: Вызывать hook в `vfs_close_fd`**

В `kernel/vfs.c` заменить тело `vfs_close_fd` (строки 465-472):

```c
int vfs_close_fd(int fd) {
    struct open_file *of = ofile_get(fd);
    if (!of) return VFS_EBADF;
    struct vfs_fs *fs = of->inode->fs;
    unsigned int ino = of->inode->ino;
    int flags = of->flags;
    vfs_put(of->inode);
    if (fs && fs->close)
        fs->close(fs, ino, flags);
    of->inode = 0;
    of->refcount = 0;
    return 0;
}
```

`fs`/`ino`/`flags` сохраняются ДО `vfs_put`, потому что для кешированных inode `vfs_put` может освободить объект.

- [ ] **Step 3: Собрать и убедиться, что ничего не сломалось**

Run: `make`
Expected: собирается без новых предупреждений `-Wall -Wextra`. sfs2/procfs не требуют правок — их `.close` остаётся NULL (designated initializer).

- [ ] **Step 4: Commit**

```bash
git add kernel/vfs.h kernel/vfs.c
git commit -m "kernel: vfs: add per-fs close hook"
```

### Task 2: pipefs + `vfs_pipe()`

**Files:**
- Create: `kernel/pipe.h`, `kernel/pipe.c`
- Modify: `kernel/vfs.c` (реализация `vfs_pipe`, include pipe.h)
- Modify: `Makefile` (KERNEL_OBJS += kernel/pipe.o)

**Interfaces:**
- Consumes: хук `close` из Task 1; `struct vfs_inode`/`struct vfs_fs`/`ofiles[]`/`VFS_O_*` из `kernel/vfs.h`.
- Produces:
  - `struct aos_pipe` (в pipe.h): `used, head, tail, count, nreaders, nwriters, buf[PIPE_BUF_SIZE], inode`.
  - `struct aos_pipe *pipe_alloc(void);` — свободный слот с `refcount=2`, `nreaders=nwriters=1`, или 0.
  - `void pipe_close(struct vfs_fs *fs, unsigned int ino, int flags);` — хук.
  - `extern struct vfs_fs pipefs_fs;`
  - `int vfs_pipe(int *rd, int *wr);` в `kernel/vfs.h` + реализация в `kernel/vfs.c`.

- [ ] **Step 1: Создать `kernel/pipe.h`**

```c
#ifndef PIPE_H
#define PIPE_H

#include "vfs.h"

#define PIPE_BUF_SIZE 4096
#define PIPE_MAX      8

struct aos_pipe {
    unsigned int used;
    unsigned int head;
    unsigned int tail;
    unsigned int count;
    unsigned int nreaders;
    unsigned int nwriters;
    unsigned char buf[PIPE_BUF_SIZE];
    struct vfs_inode inode;
};

struct aos_pipe *pipe_alloc(void);
void pipe_close(struct vfs_fs *fs, unsigned int ino, int flags);

extern struct vfs_fs pipefs_fs;

#endif
```

- [ ] **Step 2: Создать `kernel/pipe.c`**

```c
#include "pipe.h"
#include "string.h"

static struct aos_pipe pipes[PIPE_MAX];

struct aos_pipe *pipe_alloc(void) {
    for (int i = 0; i < PIPE_MAX; i++) {
        if (pipes[i].used) continue;
        struct aos_pipe *p = &pipes[i];
        p->used = 1;
        p->head = 0;
        p->tail = 0;
        p->count = 0;
        p->nreaders = 1;
        p->nwriters = 1;
        memset(&p->inode, 0, sizeof(p->inode));
        p->inode.ino = (unsigned int)i + 1;
        p->inode.fs = &pipefs_fs;
        p->inode.type = 1;
        p->inode.nlink = 1;
        p->inode.size = 0;
        p->inode.refcount = 2;
        p->inode.valid = 0;     // never cached; vfs_put releases nothing
        return p;
    }
    return 0;
}

static int pipe_read_at(struct vfs_fs *fs, unsigned int ino, void *buf,
                        unsigned int len, unsigned int off) {
    (void)fs; (void)off;
    struct aos_pipe *p = &pipes[ino - 1];
    if (len == 0) return 0;
    while (p->count == 0 && p->nwriters > 0)
        __asm__ __volatile__("sti; hlt; cli");
    if (p->count == 0) return 0;        // EOF: no writers left
    unsigned int n = len < p->count ? len : p->count;
    for (unsigned int i = 0; i < n; i++) {
        ((unsigned char *)buf)[i] = p->buf[p->tail];
        p->tail = (p->tail + 1) % PIPE_BUF_SIZE;
    }
    p->count -= n;
    return (int)n;
}

static int pipe_write_at(struct vfs_fs *fs, unsigned int ino, const void *buf,
                         unsigned int len, unsigned int off) {
    (void)fs; (void)off;
    struct aos_pipe *p = &pipes[ino - 1];
    if (len == 0) return 0;
    unsigned int done = 0;
    while (done < len) {
        if (p->nreaders == 0) return -32;   // -EPIPE
        while (p->count == PIPE_BUF_SIZE && p->nreaders > 0)
            __asm__ __volatile__("sti; hlt; cli");
        unsigned int space = PIPE_BUF_SIZE - p->count;
        unsigned int n = len - done;
        if (n > space) n = space;
        for (unsigned int i = 0; i < n; i++) {
            p->buf[p->head] = ((const unsigned char *)buf)[done + i];
            p->head = (p->head + 1) % PIPE_BUF_SIZE;
        }
        p->count += n;
        done += n;
    }
    return (int)len;
}

static int pipe_stat(struct vfs_fs *fs, unsigned int ino, struct aos_stat *st) {
    (void)fs;
    st->type = 1;
    st->size = pipes[ino - 1].count;
    st->mtime = 0;
    st->nlink = 1;
    return 0;
}

void pipe_close(struct vfs_fs *fs, unsigned int ino, int flags) {
    (void)fs;
    struct aos_pipe *p = &pipes[ino - 1];
    if (flags & VFS_O_WRONLY) {
        if (p->nwriters > 0) p->nwriters--;
    } else if (p->nreaders > 0) {
        p->nreaders--;
    }
    if (p->nreaders == 0 && p->nwriters == 0)
        p->used = 0;
}

// Stub directory ops: a pipe fd can be passed as a dirfd to openat/fstatat64,
// which resolves it via vfs_get -> fs->stat (works) and then walks the path
// with fs->lookup. These must never be NULL.
static int pipe_ro_lookup(struct vfs_fs *fs, unsigned int dir_ino,
                          const char *name, unsigned int *out_ino) {
    (void)fs; (void)dir_ino; (void)name; (void)out_ino;
    return VFS_ENOTDIR;
}

static int pipe_ro_readdir(struct vfs_fs *fs, unsigned int dir_ino,
                           unsigned int idx, char *name_out,
                           unsigned int *ino_out) {
    (void)fs; (void)dir_ino; (void)idx; (void)name_out; (void)ino_out;
    return VFS_ENOTDIR;
}

static int pipe_ro_add(struct vfs_fs *fs, unsigned int dir_ino,
                       unsigned int child_ino, const char *name) {
    (void)fs; (void)dir_ino; (void)child_ino; (void)name;
    return VFS_ENOTDIR;
}

static int pipe_ro_remove(struct vfs_fs *fs, unsigned int dir_ino,
                          const char *name, unsigned int *out_child) {
    (void)fs; (void)dir_ino; (void)name; (void)out_child;
    return VFS_ENOTDIR;
}

static int pipe_ro_mkdir(struct vfs_fs *fs, unsigned int parent_ino,
                         const char *name, unsigned int *out_ino) {
    (void)fs; (void)parent_ino; (void)name; (void)out_ino;
    return VFS_ENOTDIR;
}

static int pipe_ro_rmdir(struct vfs_fs *fs, unsigned int parent_ino,
                         const char *name) {
    (void)fs; (void)parent_ino; (void)name;
    return VFS_ENOTDIR;
}

static int pipe_ro_unlink(struct vfs_fs *fs, unsigned int dir_ino,
                          const char *name) {
    (void)fs; (void)dir_ino; (void)name;
    return VFS_ENOTDIR;
}

static int pipe_ro_truncate(struct vfs_fs *fs, unsigned int ino,
                            unsigned int newsize) {
    (void)fs; (void)ino; (void)newsize;
    return VFS_EINVAL;
}

static int pipe_ro_alloc(struct vfs_fs *fs, unsigned int type) {
    (void)fs; (void)type;
    return 0;
}

struct vfs_fs pipefs_fs = {
    .name = "pipefs",
    .read_at = pipe_read_at,
    .write_at = pipe_write_at,
    .truncate = pipe_ro_truncate,
    .lookup = pipe_ro_lookup,
    .add_dirent = pipe_ro_add,
    .remove_dirent = pipe_ro_remove,
    .mkdir = pipe_ro_mkdir,
    .rmdir = pipe_ro_rmdir,
    .unlink = pipe_ro_unlink,
    .readdir = pipe_ro_readdir,
    .stat = pipe_stat,
    .alloc_inode = pipe_ro_alloc,
    .close = pipe_close,
};
```

- [ ] **Step 3: Реализовать `vfs_pipe()` в `kernel/vfs.c`**

Добавить `#include "pipe.h"` в шапку `kernel/vfs.c` (после `#include "vfs.h"`). Добавить функцию после `vfs_open_fd`:

```c
int vfs_pipe(int *rd, int *wr) {
    int r = -1, w = -1;
    for (int i = 3; i < VFS_OFILES; i++)
        if (!ofiles[i].inode) { r = i; break; }
    if (r < 0) return VFS_EMFILE;
    for (int i = r + 1; i < VFS_OFILES; i++)
        if (!ofiles[i].inode) { w = i; break; }
    if (w < 0) return VFS_EMFILE;
    struct aos_pipe *p = pipe_alloc();
    if (!p) return VFS_EMFILE;
    ofiles[r].inode = &p->inode;
    ofiles[r].flags = VFS_O_RDONLY;
    ofiles[r].pos = 0;
    ofiles[r].refcount = 1;
    ofiles[w].inode = &p->inode;
    ofiles[w].flags = VFS_O_WRONLY;
    ofiles[w].pos = 0;
    ofiles[w].refcount = 1;
    *rd = r;
    *wr = w;
    return 0;
}
```

Добавить прототип в `kernel/vfs.h` (после `vfs_open_fd`):

```c
int vfs_pipe(int *rd, int *wr);
```

- [ ] **Step 4: Подключить `kernel/pipe.o` в Makefile**

В `Makefile` в `KERNEL_OBJS` добавить `kernel/pipe.o` (рядом с `kernel/linux_syscall.o`).

- [ ] **Step 5: Собрать**

Run: `make`
Expected: собирается без новых предупреждений.

- [ ] **Step 6: Commit**

```bash
git add kernel/pipe.h kernel/pipe.c kernel/vfs.c kernel/vfs.h Makefile
git commit -m "kernel: pipefs virtual filesystem and vfs_pipe()"
```

### Task 3: `pipe()` syscall, read(0)/write(1,2) fd routing, pipetest (TDD)

**Files:**
- Create: `tools/linux/piptest.c`, `scripts/pipetest.py`
- Modify: `kernel/syscall.h` (route_text → int), `kernel/syscall.c`, `kernel/linux_syscall.c`, `Makefile` (LINUX_EMBED)

**Interfaces:**
- Consumes: `vfs_pipe`/`vfs_ofile_ptr`/`vfs_read_fd`/`vfs_write_fd` (Task 2), `get_current_task`, `stdin_fd`/`stdout_fd`/`fds[]` (task.h), `in_luser`/`in_user`.
- Produces: `int route_text(const char *s, unsigned int len);`; syscall `pipe()` на `case 42`; read(0)/write(1,2) проброс для обоих ABI; `build/linux/piptest` в ramdisk как `lin/piptest`.

- [ ] **Step 1: Написать падающий тест `scripts/pipetest.py`**

Создать `scripts/pipetest.py` (шаблон — `scripts/cwdtest.py`):

```python
#!/usr/bin/env python3
import os
import socket
import subprocess
import sys
import time

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
ISO = os.path.join(ROOT, "aos.iso")
MON = "/tmp/aos-pipetest.sock"
SER = "/tmp/aos-pipetest.sock"

BOOT_MARKS = [
    "VFS: / [sfs2] root=1",
    "Filesystem ready.",
    "AOS>",
]

def wait_for(path, seconds=10):
    end = time.time() + seconds
    while time.time() < end:
        if os.path.exists(path): return
        time.sleep(0.05)
    raise RuntimeError("timeout waiting for " + path)

def drain(s, log, end, needle=b""):
    while time.time() < end:
        try:
            d = s.recv(4096)
            if not d: break
            log += d
            if needle and needle in log:
                break
        except socket.timeout:
            pass
    return log

def main():
    for path in (MON, SER):
        try: os.unlink(path)
        except FileNotFoundError: pass
    qemu = subprocess.Popen([
        "qemu-system-i386", "-m", "256", "-cdrom", ISO, "-display", "none",
        "-serial", "unix:" + SER + ",server,nowait",
        "-monitor", "unix:" + MON + ",server,nowait",
    ], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
    try:
        wait_for(SER)
        time.sleep(0.5)
        s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
        s.settimeout(1)
        s.connect(SER)

        # The WM registers for events shortly after the prompt and then
        # captures serial input, so the whole command sequence must be
        # queued the moment AOS> appears.
        log = drain(s, b"", time.time() + 40, b"AOS>")
        text = log.decode(errors="replace")
        if b"KERNEL PANIC" in log:
            raise AssertionError("kernel panic during boot:\n" + text[-400:])
        for mark in BOOT_MARKS:
            if mark not in text:
                raise AssertionError("serial log missing %r; tail:\n%s"
                                     % (mark, text[-400:]))
        print("PASS: boot OK")

        # pipe() syscall test: musl falls back from pipe2 to pipe (42).
        s.sendall(b"lin/piptest\n")
        out = drain(s, b"", time.time() + 30, b"PIPETEST OK")
        if b"KERNEL PANIC" in out:
            raise AssertionError("kernel panic in pipe():\n"
                                 + out[-400:].decode(errors="replace"))
        if b"PIPETEST OK" not in out:
            raise AssertionError("lin/piptest did not print PIPETEST OK; out:\n"
                                 + out[-600:].decode(errors="replace"))
        print("PASS: pipe() syscall (lin/piptest)")
        return 0
    finally:
        qemu.terminate()
        try: qemu.wait(timeout=5)
        except subprocess.TimeoutExpired: qemu.kill()

if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `python3 scripts/pipetest.py`
Expected: FAIL — `lin/piptest` ещё не в ramdisk, shell печатает «Unknown command», ассерт «did not print PIPETEST OK».

- [ ] **Step 3: Создать `tools/linux/piptest.c`**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(int argc, char **argv) {
    if (argc > 1 && strcmp(argv[1], "gen") == 0) {
        long total = (argc > 2) ? atol(argv[2]) : 20000;
        char block[512];
        memset(block, 'x', sizeof(block));
        long left = total;
        while (left > 0) {
            int chunk = (left > (long)sizeof(block)) ? (int)sizeof(block)
                                                     : (int)left;
            if (write(1, block, (unsigned int)chunk) != chunk) return 5;
            left -= chunk;
        }
        return 0;
    }

    int fd[2];
    if (pipe(fd) != 0) { printf("PIPETEST pipe failed\n"); return 1; }
    const char *msg = "hello-pipe";
    if (write(fd[1], msg, 10) != 10) { printf("PIPETEST write failed\n"); return 2; }
    close(fd[1]);
    char buf[16];
    int n = (int)read(fd[0], buf, sizeof(buf));
    if (n != 10 || memcmp(buf, msg, 10) != 0) {
        printf("PIPETEST read mismatch %d\n", n); return 3;
    }
    n = (int)read(fd[0], buf, sizeof(buf));
    if (n != 0) { printf("PIPETEST eof %d\n", n); return 4; }
    close(fd[0]);
    printf("PIPETEST OK\n");
    return 0;
}
```

(`gen`-режим используется в Task 5 для стресса >PIPE_BUF; сейчас просто компилируется.)

- [ ] **Step 4: Встроить `lin/piptest` в ramdisk**

В `Makefile` строку `LINUX_EMBED` дополнить:

```make
LINUX_EMBED = $(if $(LINUX_BINS),--data lin/hello=build/linux/hello --data lin/ls=build/linux/ls --data lin/cat=build/linux/cat --data lin/piptest=build/linux/piptest --data lin/test.txt=tools/linux/test.txt)
```

- [ ] **Step 5: `route_text` → `int`**

В `kernel/syscall.h` заменить:

```c
void route_text(const char *s, unsigned int len);
```

на:

```c
int route_text(const char *s, unsigned int len);
```

В `kernel/syscall.c` заменить сигнатуру и добавить возвраты (строки 80-107):

```c
int route_text(const char *s, unsigned int len) {
    struct task *t = get_current_task();
    if (t->stdout_fd >= 0)
        return vfs_write_fd(t->stdout_fd, s, len);
    unsigned int pid = task_current_pid();
    unsigned int sink = task_current_sink();
    if (pid > 0 && sink > 0 && task_alive(sink)) {
        for (unsigned int i = 0; i < len; i += 12) {
            unsigned int n = len - i;
            if (n > 12) n = 12;
            unsigned int b = 0, c = 0, d = 0;
            for (unsigned int j = 0; j < n; j++) {
                unsigned int byte = (unsigned char)s[i + j];
                if (j < 4)
                    b |= byte << (8 * j);
                else if (j < 8)
                    c |= byte << (8 * (j - 4));
                else
                    d |= byte << (8 * (j - 8));
            }
            task_mailbox_send(sink, MSG_DATA, n, b, c, d);
        }
        return 0;
    }
    terminal_write(s, len);
    return 0;
}
```

Прочие вызовы `route_text` (SYS_PRINT, route_hex, route_dec, SYS_PUTCHAR) игнорируют возврат — менять их не нужно.

- [ ] **Step 6: AOS `SYS_READ` fd 0 — проброс через `stdin_fd`**

В `kernel/syscall.c` в ветке `case SYS_READ`, блок `if (fd == 0)` (строки 216-227), после `in_user`-проверки добавить:

```c
            if (get_current_task()->stdin_fd >= 0) {
                r->eax = vfs_read_fd(get_current_task()->stdin_fd, buf, len);
                break;
            }
```

Итоговый блок fd==0:

```c
        if (fd == 0) {
            if (len == 0 || !in_user(buf, len)) {
                r->eax = -5;
                break;
            }
            if (get_current_task()->stdin_fd >= 0) {
                r->eax = vfs_read_fd(get_current_task()->stdin_fd, buf, len);
                break;
            }
            int k = terminal_read_key();
            if (k < 0) {
                r->eax = -1;             // non-blocking, like SYS_READ_KEY
            } else {
                ((char *)buf)[0] = (char)k;
                r->eax = 1;
            }
        }
```

- [ ] **Step 7: AOS `SYS_WRITE` fd 1/2 — проброс EPIPE**

В `kernel/syscall.c` ветка `if (fd == 1 || fd == 2)` (строки 247-249):

```c
        if (fd == 1 || fd == 2) {
            int rc = route_text(buf, len);
            r->eax = (rc < 0) ? rc : (int)len;
        }
```

- [ ] **Step 8: Linux write fd ≤ 2 — проброс EPIPE**

В `kernel/linux_syscall.c` `case 4` (строки 120-122):

```c
        if (fd <= 2) {
            int rc = route_text(buf, count);
            r->eax = (rc < 0) ? rc : count;
        }
```

- [ ] **Step 9: Linux read fd 0 — проброс через `stdin_fd`**

В `kernel/linux_syscall.c` `case 3` блок `if (fd == 0)` (строки 264-269):

```c
        if (fd == 0) {
            if (count == 0) { r->eax = 0; break; }
            if (get_current_task()->stdin_fd >= 0) {
                r->eax = vfs_read_fd(get_current_task()->stdin_fd, buf, count);
                break;
            }
            int k = terminal_read_key();
            r->eax = (k < 0) ? -11 : 1;                     // -EAGAIN when empty
            if (k >= 0) buf[0] = (char)k;
            break;
        }
```

- [ ] **Step 10: syscall `pipe()` — `case 42`**

В `kernel/linux_syscall.c` добавить case в `switch (n)` (рядом с `case 3`):

```c
    case 42: {  // pipe(int fds[2])
        unsigned int *fds = (unsigned int *)r->ebx;
        if (!in_luser(fds, 8)) { r->eax = -14; break; }    // -EFAULT
        int rd, wr;
        int rc = vfs_pipe(&rd, &wr);
        if (rc < 0) { r->eax = rc; break; }
        struct task *t = get_current_task();
        t->fds[rd] = vfs_ofile_ptr(rd);
        t->fds[wr] = vfs_ofile_ptr(wr);
        fds[0] = (unsigned int)rd;
        fds[1] = (unsigned int)wr;
        r->eax = 0;
        break;
    }
```

`trace_record`/`trace_finish` логируют его автоматически — менять ничего не нужно.

- [ ] **Step 11: Собрать и запустить тест — должен пройти**

Run: `make && python3 scripts/pipetest.py`
Expected: PASS — «boot OK» и «PASS: pipe() syscall (lin/piptest)».

- [ ] **Step 12: Commit**

```bash
git add tools/linux/piptest.c scripts/pipetest.py kernel/syscall.h kernel/syscall.c kernel/linux_syscall.c Makefile
git commit -m "kernel: pipe() syscall, stdin/stdout fd wiring, EPIPE passthrough"
```

### Task 4: `lin/cat` stdin-режим + shell `exec_pipe` (TDD)

**Files:**
- Modify: `tools/linux/cat.c`, `kernel/commands.c`, `scripts/pipetest.py`

**Interfaces:**
- Consumes: `vfs_pipe`/`vfs_close_fd`/`vfs_ofile_ptr` (Task 2), `task_spawn`/`task_slot`/`task_waitpid`/`TASK_MAX_FDS` (task.h), `find_operator`/`cmd_is_builtin`/`path_resolve`/`shell_set_status`/`terminal_*` (commands.c/terminal.h).
- Produces: `exec_pipe(const char *line)` — статическая функция в commands.c; `lin/cat` без аргументов читает stdin.

- [ ] **Step 1: Расширить `scripts/pipetest.py` пайплайн-тестами**

Вставить в `main()` после блока «pipe() syscall test» и перед `return 0`:

```python
        # Pipeline: AOS writer (ls) -> Linux reader (lin/cat) through a pipe.
        s.sendall(b"ls | lin/cat\n")
        out = drain(s, b"", time.time() + 30, b"AOS>")
        if b"KERNEL PANIC" in out:
            raise AssertionError("kernel panic in 'ls | lin/cat':\n"
                                 + out[-400:].decode(errors="replace"))
        otext = out.decode(errors="replace")
        if b"uptime" not in out:
            raise AssertionError("'ls | lin/cat' did not print the bin list; "
                                 "out:\n" + otext[-600:])
        print("PASS: ls | lin/cat")

        # Pipeline with a procfs source.
        s.sendall(b"cat /proc/uptime | lin/cat\n")
        out = drain(s, b"", time.time() + 30, b"AOS>")
        if b"KERNEL PANIC" in out:
            raise AssertionError("kernel panic in 'cat /proc/uptime | lin/cat':\n"
                                 + out[-400:].decode(errors="replace"))
        if b"uptime" not in out or b"ticks" not in out:
            raise AssertionError("'cat /proc/uptime | lin/cat' missing output; "
                                 "out:\n" + out[-600:].decode(errors="replace"))
        print("PASS: cat /proc/uptime | lin/cat")
```

- [ ] **Step 2: Запустить тест, убедиться что падает**

Run: `make && python3 scripts/pipetest.py`
Expected: FAIL — пайплайн `ls | lin/cat` использует старый OP_PIPE (temp-file `/pipe_tmp`): первая часть пишет в файл, но `lin/cat` без аргумента печатает usage, а pipe-файла в ramdisk нет → вывод не содержит «uptime».

- [ ] **Step 3: `lin/cat` без аргументов читает stdin**

В `tools/linux/cat.c` заменить `main`:

```c
int main(int argc, char **argv) {
    if (argc < 2) {
        int c;
        while ((c = getchar()) != EOF)
            putchar(c);
        return 0;
    }
    for (int i = 1; i < argc; i++) {
        FILE *f = fopen(argv[i], "r");
        if (!f) {
            printf("cat: %s: No such file or directory\n", argv[i]);
            return 1;
        }
        int c;
        while ((c = fgetc(f)) != EOF)
            putchar(c);
        fclose(f);
    }
    return 0;
}
```

(существующий тест `lincat.py` зовёт `lin/cat` с файлом — не сломается).

- [ ] **Step 4: `exec_pipe()` в `kernel/commands.c`**

Добавить макрос рядом с `OP_PIPE` (строка 335) и функцию после `find_operator`:

```c
#define PIPE_STAGES_MAX 8

// Pipeline execution: "a | b | c". Every stage runs as its own task wired to
// N-1 pipes; the shell (task 0) waits for all stages. $? = last stage's code.
static void exec_pipe(const char *line) {
    char parts[PIPE_STAGES_MAX][LINE_BUF_SIZE];
    char full[PIPE_STAGES_MAX][PATH_MAX];
    char args[PIPE_STAGES_MAX][LINE_BUF_SIZE];
    unsigned int pids[PIPE_STAGES_MAX];
    int rd[PIPE_STAGES_MAX - 1], wr[PIPE_STAGES_MAX - 1];
    int n = 0;

    // Split on '|' operators (find_operator requires a space before them).
    const char *cur = line;
    for (;;) {
        int op;
        const char *pos = find_operator(cur, &op);
        unsigned int len = pos ? (unsigned int)(pos - cur) : 0;
        if (!pos) { while (cur[len]) len++; }
        while (len > 0 && cur[len - 1] == ' ') len--;
        const char *s = cur;
        while (*s == ' ') s++;
        unsigned int sl = (unsigned int)(s - cur);
        if (n >= PIPE_STAGES_MAX) {
            terminal_print("\npipe: too many stages");
            return;
        }
        unsigned int cplen = (len > sl) ? len - sl : 0;
        if (cplen >= LINE_BUF_SIZE) cplen = LINE_BUF_SIZE - 1;
        for (unsigned int i = 0; i < cplen; i++) parts[n][i] = s[i];
        parts[n][cplen] = '\0';
        n++;
        if (!pos) break;
        cur = pos + 1;
    }
    if (n < 2) { terminal_print("\npipe: empty pipeline"); return; }

    // Validate every stage before spawning anything: no builtins, no
    // redirects, and every command must resolve through PATH.
    for (int i = 0; i < n; i++) {
        const char *p = parts[i];
        const char *a = p;
        while (*a && *a != ' ') a++;
        unsigned int cl = (unsigned int)(a - p);
        while (*a == ' ') a++;
        char cmd[16];
        unsigned int cc = cl < 15 ? cl : 15;
        for (unsigned int k = 0; k < cc; k++) cmd[k] = p[k];
        cmd[cc] = '\0';
        if (cmd_is_builtin(cmd)) {
            terminal_print("\npipe: builtin not supported in pipeline");
            return;
        }
        int op;
        if (find_operator(parts[i], &op)) {
            terminal_print("\npipe: redirect not supported in pipe part");
            return;
        }
        if (!path_resolve(cmd, full[i], sizeof(full[i]))) {
            terminal_print("\nUnknown command: ");
            terminal_write(cmd, cc);
            terminal_print(". Type 'help'");
            shell_set_status(127);
            return;
        }
        unsigned int al = 0;
        while (a[al]) al++;
        if (al >= LINE_BUF_SIZE) al = LINE_BUF_SIZE - 1;
        for (unsigned int k = 0; k < al; k++) args[i][k] = a[k];
        args[i][al] = '\0';
    }

    // Create the N-1 pipes (global ofiles[] slots, inode refcount 2 each).
    for (int i = 0; i < n - 1; i++) {
        if (vfs_pipe(&rd[i], &wr[i]) != 0) {
            for (int j = 0; j < i; j++) { vfs_close_fd(rd[j]); vfs_close_fd(wr[j]); }
            terminal_print("\npipe: cannot create pipe");
            return;
        }
    }

    // Spawn all stages. Wire each child's stdin/stdout to its pipe ends
    // right after spawn (the scheduler has not run the child yet).
    for (int i = 0; i < n; i++) {
        unsigned int pid;
        if (task_spawn(full[i], args[i], 0, &pid) != 0) {
            for (int j = 0; j < n - 1; j++) { vfs_close_fd(rd[j]); vfs_close_fd(wr[j]); }
            terminal_print("\npipe: spawn failed");
            shell_set_status(1);
            return;
        }
        pids[i] = pid;
        struct task *c = task_slot(pid);
        if (i > 0) {
            c->fds[rd[i - 1]] = vfs_ofile_ptr(rd[i - 1]);
            c->stdin_fd = rd[i - 1];
        }
        if (i < n - 1) {
            c->fds[wr[i]] = vfs_ofile_ptr(wr[i]);
            c->stdout_fd = wr[i];
        }
    }

    // Wait for every stage; $? is the last stage's exit code.
    int last = 0;
    for (int i = 0; i < n; i++)
        last = task_waitpid(pids[i]);
    shell_set_status(last);
}
```

- [ ] **Step 5: Подключить `exec_pipe` в ветку OP_PIPE**

В `kernel/commands.c` заменить весь блок `if (op == OP_PIPE) { ... }` (строки 504-537, temp-file логика) на:

```c
    if (op == OP_PIPE) {
        exec_pipe(line);
        return;
    }
```

`line` — исходная строка со всем пайплайном (переменная уже доступна в `exec_stage`).

- [ ] **Step 6: Собрать и запустить тест — должен пройти**

Run: `make && python3 scripts/pipetest.py`
Expected: PASS — все три stage («pipe() syscall», «ls | lin/cat», «cat /proc/uptime | lin/cat»).

- [ ] **Step 7: Проверить ручную регрессию пайплайна из трёх стадий**

Run: `make debug`, в serial-консоли (или через `scripts/shelltest.py`-стиль burst):
```
ls | lin/cat | lin/cat
cat /proc/uptime | lin/cat
```
Expected: без «KERNEL PANIC», второй пайплайн печатает «uptime N ticks».

- [ ] **Step 8: Commit**

```bash
git add tools/linux/cat.c kernel/commands.c scripts/pipetest.py
git commit -m "shell: multi-stage pipelines (a | b | c)"
```

### Task 5: Стресс >PIPE_BUF, регрессия, документация

**Files:**
- Modify: `scripts/pipetest.py`, `Makefile` (LINUX_TESTS), `AGENTS.md`

**Interfaces:**
- Consumes: `lin/piptest gen N` (Task 3), `exec_pipe` (Task 4).
- Produces: `make test` включает `pipetest`; AGENTS.md описывает пайпы.

- [ ] **Step 1: Добавить в `pipetest.py` стресс-тест блокирующего writer**

Вставить в `main()` после блока «cat /proc/uptime | lin/cat» и перед `return 0`:

```python
        # Stress: 20000 bytes through a 4096-byte pipe buffer. The writer
        # blocks when full; lin/cat drains until EOF.
        s.sendall(b"lin/piptest gen 20000 | lin/cat\n")
        out = drain(s, b"", time.time() + 60, b"AOS>")
        if b"KERNEL PANIC" in out:
            raise AssertionError("kernel panic in pipe stress:\n"
                                 + out[-400:].decode(errors="replace"))
        if b"PIPETEST" in out:
            raise AssertionError("unexpected PIPETEST marker in stress output")
        print("PASS: lin/piptest gen 20000 | lin/cat (blocking writer)")
```

(маркер «PIPETEST» проверяет, что gen-режим не свалился в основной тест — вывод содержит только 'x'.)

- [ ] **Step 2: Запустить тест**

Run: `make && python3 scripts/pipetest.py`
Expected: PASS — все 5 проверок. Если writer не блокировался бы или reader завис, тест упал бы по таймауту `drain`.

- [ ] **Step 3: Добавить `pipetest` в `make test`**

В `Makefile` строку `LINUX_TESTS` заменить:

```make
LINUX_TESTS = $(if $(LINUX_BINS),linhello lincat lindirtest pipetest)
```

- [ ] **Step 4: Полный регрессионный прогон**

Run: `make test`
Expected: ALL 22 TESTS PASSED (все прежние регрессии + pipetest). Замечание: QEMU GUI-тесты тайминговые под TCG — при сбое перезапустить один раз.

- [ ] **Step 5: Обновить `AGENTS.md`**

Добавить в раздел про shell/программы краткую секцию про пайпы (на русском):

```markdown
### Pipes (kernel shell)

- `a | b | c` runs every stage as its own task wired through N-1 real pipes;
  the shell (task 0) waits for all stages; `$?` = last stage's exit code.
- `kernel/pipe.c` implements pipefs: a static array of 8 ring buffers
  (`PIPE_BUF_SIZE=4096`, head/tail/count, `nreaders`/`nwriters`). Pipe inodes
  are `valid=0` (never in the VFS cache), `refcount=2`, freed when both ends
  close (`vfs_fs.close` hook, `pipe_close`).
- `int vfs_pipe(int *rd, int *wr)` (`kernel/vfs.c`) allocates two global
  `ofiles[]` slots (fd >= 3) pointing at the pipe inode. `pipe()` = Linux ABI
  syscall 42 (`kernel/linux_syscall.c`); musl falls back from pipe2.
- Blocking: `pipe_read_at` spins `sti; hlt; cli` while empty and writers
  exist; `pipe_write_at` spins while full and readers exist. Tasks stay
  `TASK_RUNNING`, so the round-robin scheduler reschedules them (<=1 ms
  latency). EOF = 0 when no writers; write to a pipe with no readers returns
  `-EPIPE` (propagated by `route_text`/write for both ABIs).
- read(0) routes through `t->stdin_fd` when set (both AOS `SYS_READ` and
  Linux `read`); `terminal_read_key()` is untouched.
- Known limits: fd duplication of pipe ends is not tracked, no SIGPIPE,
  busy-poll blocking, pipes are foreground-only (`a | b &` rejected), no
  pipe2/pipe AOS syscall.
```

- [ ] **Step 6: Commit**

```bash
git add scripts/pipetest.py Makefile AGENTS.md
git commit -m "test: pipe stress >PIPE_BUF and full regression; docs: pipes"
```

---

## Self-Review

**Spec coverage (по секциям спеки `2026-08-09-pipes-design.md`):**
- pipefs (`PIPE_BUF_SIZE`/`PIPE_MAX`, ring, nreaders/nwriters, inode valid=0/ino=index+1/refcount=2) → Task 2.
- VFS hook `close` + `vfs_pipe` → Task 1 (hook), Task 2 (vfs_pipe, pipefs).
- read/write пути: read(0) через stdin_fd в обоих ABI → Task 3 (Steps 6, 9); write(1/2) EPIPE-проброс → Task 3 (Steps 5, 7, 8); writev EPIPE игнорируется → дизайн не меняется, явной задачи нет (в скоупе спеки: «игнорируется»).
- syscall `pipe()` 42 + userland fds → Task 3 (Step 10).
- Shell `exec_pipe`: сплит, валидация builtin/redirect/path, N-1 pipes, spawn + подвязка, waitpid, `$?` → Task 4 (Steps 4-5).
- Тесты: piptest (syscall) → Task 3; `ls | lin/cat`, `cat /proc/uptime | lin/cat` → Task 4; стресс >PIPE_BUF → Task 5; `make test` + AGENTS.md → Task 5.
- Out-of-scope (dup/SIGPIPE/bg/GUI/pipe2) соблюдается — не реализуется.

**Placeholder scan:** нет TBD/TODO; каждый шаг с кодом содержит полный код. Единственный ручной шаг — Task 4 Step 7 (проверка трёхстадийного пайплайна в `make debug`) — это опциональная проверка, не обязательная для прохождения `pipetest.py`.

**Type consistency:** `route_text` везде `int` (syscall.h, syscall.c, linux_syscall.c). `vfs_pipe(int*, int*)`, `pipe_alloc(void)`, `pipe_close(vfs_fs*, unsigned int, int)` — одинаковые в pipe.h/vfs.h/реализациях. `exec_pipe(const char*)` — статическая, вызывается один раз. `PIPE_STAGES_MAX` в commands.c не конфликтует с `PIPE_MAX` в pipe.h.
