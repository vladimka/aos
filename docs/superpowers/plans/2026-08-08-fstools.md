# [fstools] Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 6 готовых musl-утилит (`cp`/`mv`/`mkdir`/`rmdir`/`head`/`wc`) собираются в ramdisk как `bin/*`, работают из шелла, покрыты E2E-тестом.

**Architecture:** добавляем имена в `PROGRAMS` (собираются существующим правилом `build/prog/%.elf`), ramdisk растёт 1→2 МБ (6 бинарей ≈ 200 КБ не влезают в 158 КБ свободных), чиним `head.c` (опциональное число строк), новый headless-тест `scripts/fstoolstest.py` по образцу `shelltest.py`.

**Tech Stack:** musl i386 (`-static -no-pie -Os`), `kernel/block.h`, `scripts/gen_progs.py`, `scripts/qtest.py`.

## Global Constraints

- `kernel/progs.c` — генерируемый, руками не трогать.
- Все правки — только перечисленные файлы; коммиты по одному на задачу.
- QEMU/UI-тесты запускает человек (`make test`); ассистент — только `make` и статичные проверки.
- `make` без warning'ов (`-Wall -Wextra` в правиле `build/prog/%.elf`).
- Файлы на диске/десктопе: вновь созданные `/t.txt` и т.п. появятся иконками на десктопе WM — это ожидаемо, не ассертится.

---

## Task 1: Сборка утилит + рост ramdisk + фикс head.c

**Files:**
- Modify: `Makefile:24` (PROGRAMS), `kernel/block.h:8` (RAMDISK_SECTORS), `programs/musl/head.c:7`.

**Interfaces:**
- Produces: `bin/cp`, `bin/mv`, `bin/mkdir`, `bin/rmdir`, `bin/head`, `bin/wc` в ramdisk (потребитель — Task 2).

- [ ] **Step 1: Добавить утилиты в PROGRAMS**

```
PROGRAMS = help uptime clear echo tick info reboot panic ls cat rm format shutdown test wm term clock date ipctest notepad many linrun sleeptest exitto random fstest procinfo bgspawn cp mv mkdir rmdir head wc
```

- [ ] **Step 2: Расширить ramdisk до 2 МБ**

`kernel/block.h:8`: `#define RAMDISK_SECTORS 4096` (было 2048). Резерв pmm (pmm.c:256) и размер SFS (sfs2.c, block.c) берутся из макроса автоматически; staging-буфер на 0xC00000 не задет; лимит `_end <= 0x400000` не нарушится.

- [ ] **Step 3: Фикс `head.c` — опциональное число строк**

```c
if (argc < 2) {          // было: argc < 3 — мешало запуску "head <file>"
    printf("\nUsage: head <file> [lines]");
    return 0;
}
int lines = 10;
if (argc > 2) lines = atoi(argv[2]);
```

- [ ] **Step 4: Пересобрать и проверить**

Run: `make`
Expected: чистая сборка; `nm -n aos.elf | grep -w _end` < `0x00400000` (ожидаемо ~0x33xxxx); `kernel/progs.c` перегенерирован (содержит `bin/cp`..`bin/wc`).

- [ ] **Step 5: Commit**

```bash
git add Makefile kernel/block.h programs/musl/head.c
git commit -m "progs: build cp/mv/mkdir/rmdir/head/wc, grow ramdisk to 2MB, head default lines"
```

## Task 2: E2E-тест fstoolstest.py + доки

**Files:**
- Create: `scripts/fstoolstest.py`
- Modify: `Makefile:130` (TESTS), `TODO.md`, `AGENTS.md`

**Interfaces:**
- Consumes: `bin/cp`…`bin/wc` из Task 1; `QTest` из `scripts/qtest.py`.

- [ ] **Step 1: Создать `scripts/fstoolstest.py`** (по образцу `shelltest.py`, serial_mode="socket")

```python
#!/usr/bin/env python3
"""E2E FS-tools test: cp/mv/mkdir/rmdir/head/wc via foreground redirects."""
import sys

from qtest import QTest


def main():
    with QTest("fstoolstest", serial_mode="socket") as q:
        s = q.serial_socket()
        q.boot_and_ready(socket=s)

        burst = (b"echo one > /t.txt\n"
                 b"echo two >> /t.txt\n"
                 b"echo three >> /t.txt\n"
                 b"wc /t.txt\n"
                 b"head /t.txt 1\n"
                 b"cp /t.txt /t2.txt\n"
                 b"cat /t2.txt\n"
                 b"mv /t2.txt /t3.txt\n"
                 b"cat /t3.txt\n"
                 b"rm /t3.txt\n"
                 b"cat /t3.txt\n"
                 b"mkdir /d\n"
                 b"rmdir /d\n"
                 b"echo fstools-done\n")
        s.sendall(burst)
        out = q.serial_drain(s, timeout=30, needle=b"fstools-done")
        if b"KERNEL PANIC" in out:
            raise AssertionError("kernel panic during fs-tools commands:\n"
                                 + out[-400:].decode(errors="replace"))
        otext = out.decode(errors="replace")

        failures = []
        if "\n3 3 14 /t.txt\n" not in otext:
            failures.append("wc counted wrong lines/words/bytes")
        if otext.count("\none\n") < 2:   # head + cat /t2.txt (cat /t3.txt даёт 3-ю)
            failures.append("head did not print first line (or cat lost it)")
        if "Copied: /t.txt -> /t2.txt" not in otext:
            failures.append("cp failed")
        if "\nthree\n" not in otext:
            failures.append("cat /t2.txt did not show full copy")
        if "Moved: /t2.txt -> /t3.txt" not in otext:
            failures.append("mv failed")
        if "\n3 3 14 /t3.txt\n" not in otext:
            failures.append("wc /t3.txt after mv failed")
        if "File not found: /t3.txt" not in otext:
            failures.append("rm did not remove /t3.txt")
        if "Created: /d" not in otext:
            failures.append("mkdir failed")
        if "Removed: /d" not in otext:
            failures.append("rmdir failed")

        if failures:
            raise AssertionError("; ".join(failures) + ";\nout:\n" + otext[-800:])
    print("PASS: cp, mv, mkdir, rmdir, head, wc, redirects")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

Почему asserts именно такие: foreground-redirect работает — `exec_stage` (commands.c:468) ставит `t->stdout_fd` на task 0, а Linux `write(fd<=2)` → `route_text()` (syscall.c:80) пишет в файл; `wc` выдаёт `\n3 3 14 /t.txt` ("one\ntwo\nthree\n" = 14 байт); `head` печатает только "one"; `cat` после `mv` доказывает перенос; "File not found" после `rm`.

- [ ] **Step 2: Добавить в TESTS**

`Makefile:130`: `... shelltest panictest fstoolstest $(LINUX_TESTS)`

- [ ] **Step 3: Проверить синтаксис и собрать**

Run: `python3 -m py_compile scripts/fstoolstest.py && make`
Expected: py_compile без ошибок, сборка чистая.

- [ ] **Step 4: Обновить доки**

- `TODO.md`, п.3.1: отметить `cp`, `mv`, `mkdir`, `rmdir`, `head`, `wc` сделанными (mv — через копию+unlink, отдельного SFS `rename` не делаем; `tail`/`sort` — не входят).
- `AGENTS.md`: «1 MB ramdisk» → «2 MB ramdisk»; добавить новые команды в список программ.

- [ ] **Step 5: Прогон теста (человек) и commit**

Run: `python3 scripts/fstoolstest.py` (или `make test`)
Expected: `PASS: cp, mv, mkdir, rmdir, head, wc, redirects` и `ALL <N> TESTS PASSED`.

```bash
git add scripts/fstoolstest.py Makefile TODO.md AGENTS.md
git commit -m "tests: fstoolstest E2E for cp/mv/mkdir/rmdir/head/wc"
```
